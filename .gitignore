# generated by rake
flux/config-ssh-credentials.yaml
gitserver/authorized_keys
gitserver/ssh_host_key
gitserver/ssh_host_key.pub
id
id.pub
config/.git

# ignore everything added under `config/default` by default
#
# add manifests explicitly if you wish to keep them
# i.e., `git add -f config/default/STUFF`
config/default/*
